<Window x:Class="WindowLock.RecentActivityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recent installations — Window Lock" Width="900" Height="600" MinWidth="720" MinHeight="460"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Background="#F4F7FA"
        FontFamily="Segoe UI Variable Text, Segoe UI">
  <Grid Margin="26">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <StackPanel Margin="0,0,0,16"><TextBlock Text="Recent installations" FontSize="28" FontWeight="Bold" Foreground="#102A43"/><TextBlock Text="Dated traditional application records from the last 90 days. Store packages and installers that do not record a date are not yet included." Foreground="#486581" FontSize="14" TextWrapping="Wrap" Margin="0,7,0,0"/><TextBlock x:Name="CountText" Foreground="#627D98" FontSize="13" Margin="0,7,0,0"/></StackPanel>
    <DataGrid x:Name="ActivityGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="White" BorderBrush="#D9E2EC" RowHeaderWidth="0">
      <DataGrid.Columns><DataGridTextColumn Header="Installed" Binding="{Binding Installed}" Width="110"/><DataGridTextColumn Header="Application" Binding="{Binding Name}" Width="2*"/><DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="*"/><DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="130"/><DataGridTextColumn Header="Scope" Binding="{Binding Scope}" Width="110"/></DataGrid.Columns>
    </DataGrid>
    <Button Grid.Row="2" Content="Close" HorizontalAlignment="Right" Width="110" Background="#102A43" Foreground="White" Margin="0,16,0,0" Click="Close_Click"/>
  </Grid>
</Window>
